import { InputEllipsisPipe } from './input-ellipsis.pipe';

describe('InputEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new InputEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
